#include <iostream>
#include <cmath>
#include<string>

using namespace std;

int main(){
    string str;

    int i,mod,w, p = 1, q = 10;
    
    for(i = p; i <= q; i++){
        w = pow(i,2);
        str= to_string(w);
        mod = str.size() / 2;

        
    }

    return 0;
}